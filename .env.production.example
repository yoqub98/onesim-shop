# ============================================================
# PRODUCTION ENVIRONMENT — .env.production
# Copy this file to .env.production and fill in real values
# NEVER commit .env.production to git
# ============================================================

# --- FRONTEND (baked into bundle at build time) ---

# Supabase — public anon key, safe to bundle
REACT_APP_SUPABASE_URL=https://your-project.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-supabase-anon-key

# API base — leave unset in production → defaults to /api
# REACT_APP_API_BASE=

# --- BACKEND ONLY (Vercel env vars — never use REACT_APP_ prefix) ---

# eSIMAccess credentials
ESIMACCESS_API_KEY=your-esimaccess-api-key
ESIMACCESS_SECRET_KEY=your-esimaccess-secret-key

# Pricing (server-side only — must NOT be in REACT_APP_*)
USD_TO_UZS_RATE=12000
PROFIT_MARGIN=50

# Supabase service role (server-side only)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Webhook / sync protection
SYNC_SECRET_KEY=your-sync-secret-key

# Email (SMTP)
SMTP_HOST=your-smtp-host
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
